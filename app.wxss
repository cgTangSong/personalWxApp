/**app.wxss**/
/* page {
    width: 100vw;
    height: 100vh;
    background: rgb(240, 240, 240);
} */

page {
    width: 100vw;
    height: auto;
    background: rgb(240, 240, 240);
}

.container {
    width: 100%;
    height: 100%;
    font-size: 11pt;
}

button {
    padding: 0;
    margin: 0;
    background-color: inherit;
    position: static;
}

button:after {
    /* border-color: white; */
    content: none;
}

button::after {
    border: none;
}
